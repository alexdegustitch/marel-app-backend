package com.aleksandarparipovic.marel_app.search.dto;

/** Minimal sample-order row for the global search. */
public interface SampleOrderSearchRow {
    Long getId();
    String getName();
    String getCode();
    String getCustomerName();
}
