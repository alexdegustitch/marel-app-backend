package com.aleksandarparipovic.marel_app.notification_delivery;

public enum NotificationChannel {
    /** Delivered to the in-app notification centre. */
    IN_APP,
    /** Delivered by email to a snapshot address. */
    EMAIL
}
